{
  "name": "debug-dap-mcp",
  "version": "0.1.0",
  "description": "MCP stdio server exposing DAP (Debug Adapter Protocol) capabilities to any MCP client",
  "files": [
    "dist",
    "adapters",
    "README.md"
  ],
  "type": "module",
  "bin": {
    "debug-dap-mcp": "dist/index.js"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.test.json --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "jsonc-parser": "^3.3.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
